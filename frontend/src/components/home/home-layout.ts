/** Matches `HomeAppBar` horizontal padding — keep promo + shop on this gutter. */
export const HOME_GUTTER_CLASS = 'px-3';

/** 10px vertical gap between hero blocks (banner ↔ shop card). */
export const HOME_HERO_GAP_CLASS = 'gap-2.5';
