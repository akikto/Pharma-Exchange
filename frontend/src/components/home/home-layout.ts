/** Shared horizontal inset for home feed, app bar, and aligned blocks (12px). */
export const HOME_GUTTER_CLASS = 'px-3';

/** Shared outer shell for neutral home cards. */
export const HOME_CARD_SHELL_CLASS =
  'w-full max-w-full box-border rounded-[var(--radius-md)] border border-border-subtle bg-surface-raised overflow-hidden';

/** Accent-themed shell for the home promo banner carousel. */
export const HOME_PROMO_BANNER_SHELL_CLASS =
  'w-full max-w-full box-border rounded-[var(--radius-md)] border border-accent/35 border-l-4 border-l-accent bg-accent/5 overflow-hidden shadow-elevation-1';

/** 12px space below promo banner before the next hero block (Shop Card when bulk is hidden). */
export const HOME_PROMO_BANNER_SPACING_CLASS = 'mb-3';
