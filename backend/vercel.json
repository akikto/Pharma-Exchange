{
  "version": 2,
  "installCommand": "cd .. && npm ci",
  "buildCommand": "npx prisma generate && npm run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "memory": 1024
    }
  }
}
